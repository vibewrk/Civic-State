# Decisions

## 2026-06-23 [evidence: worker current_date] — EIR soul upgrade (wrk.dog)

Changed the root soul from missing/thin artifacts into a VC-grade but honesty-labeled diligence package: `BUSINESS.md`, `ROADMAP.md`, `DECISIONS.md`, and `.wrkdog-gate.json` [evidence: worker task scope].

Scrutiny gaps addressed: business-file, roadmap-file, decisions-file, gate-json, thesis-current, financial-figures, percent-figures, market-sizing, customer-definition, revenue-model, competition, risks-antiplan, assumption-ledger, go-to-market, honesty-labels, doc-dates, doc-structure, evidence-sources, freshness-stale, and roadmap-buildable-shape [evidence: worker dispatch].

What changed: the plan now treats the repo as a CivicState civic-letter workflow because that is what the code and planning files evidence [evidence: `package.json`; evidence: `.planning/PROJECT.md`; evidence: `apps/api/src/index.ts`]. It also explicitly flags the registry mismatch: the dispatch project is `brooks-history`, while the local product identity is CivicState [evidence: registry dispatch note; evidence: `package.json`].

What remains assumption and needs operator or market validation: whether this should be pitched as a business at all, whether users will pay `$5` to `$25`, whether preview-to-checkout conversion can reach `3%`, whether government inbox placement can reach `85%`, whether federal/state coverage can reach `95%`, whether local coverage can reach `60%`, whether chargebacks stay below `0.5%`, and whether legal-adjacent human review fits low-ticket economics [evidence: `.planning/PROJECT.md`; evidence: `apps/api/src/routes/payments.ts`; assumption: no production metrics found].

Decision: keep the asset on watchlist as a validation project, not an adopted investible thesis, until operator identity/business-intent ruling and paid beta evidence exist [assumption: EIR recommendation].
