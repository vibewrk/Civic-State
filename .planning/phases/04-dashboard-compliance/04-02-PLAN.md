---
phase: 04-dashboard-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/admin/page.tsx
  - apps/web/app/admin/layout.tsx
  - apps/web/app/admin/flagged/page.tsx
  - apps/web/app/admin/treasury/page.tsx
  - apps/web/app/admin/officials/page.tsx
  - apps/web/app/admin/jobs/page.tsx
  - apps/api/src/routes/admin.ts
  - apps/api/src/index.ts
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02
  - ADMN-03
  - ADMN-04
  - ADMN-05
  - ADMN-06
  - ADMN-07
  - ADMN-08
---

<objective>
Build admin tools: flagged submission review queue, treasury dashboard, Bull Board job monitor, officials maintenance, admin action logging, and auto-escalation alerts.

Purpose: The operator manages exceptions — flagged submissions, treasury discrepancies, bounced officials. Admin tools must minimize daily time to <30 minutes.
Output: Working admin dashboard with all operator tools.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create admin API endpoints</name>
  <files>apps/api/src/routes/admin.ts, apps/api/src/index.ts</files>
  <action>
Create apps/api/src/routes/admin.ts with Clerk RBAC admin check:
- GET /api/admin/flagged — list flagged submissions with reason (ADMN-02)
- POST /api/admin/flagged/:id/approve — approve flagged submission (ADMN-03)
- POST /api/admin/flagged/:id/reject — reject with reason (ADMN-03)
- POST /api/admin/flagged/:id/edit — edit and approve (ADMN-03)
- GET /api/admin/treasury — balance, today's revenue/costs, ledger entries (ADMN-04)
- GET /api/admin/officials — officials list with bounce rates, opt-out status (ADMN-06)
- PATCH /api/admin/officials/:id — update official info (ADMN-06)
- All actions logged to audit trail with HMAC (ADMN-07)
Register in index.ts. Use requireAdmin() middleware.
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire Bull Board for job queue monitoring</name>
  <files>apps/api/src/index.ts</files>
  <action>
Wire Bull Board UI at /api/admin/queues (ADMN-05):
- Uses @bull-board/express (already in dependencies)
- Shows classifier, researcher, drafter, delivery, treasury, reconciliation queues
- Protected by admin middleware
  </action>
</task>

<task type="auto">
  <name>Task 3: Build admin frontend pages</name>
  <files>apps/web/app/admin/layout.tsx, apps/web/app/admin/page.tsx, apps/web/app/admin/flagged/page.tsx, apps/web/app/admin/treasury/page.tsx, apps/web/app/admin/officials/page.tsx</files>
  <action>
1. Admin layout with sidebar navigation (Flagged Queue, Treasury, Officials, Jobs)
2. Admin dashboard with summary metrics
3. Flagged queue page: list flagged submissions, approve/reject/edit buttons (ADMN-02, ADMN-03)
4. Treasury page: balance, today's P&L, recent ledger entries (ADMN-04)
5. Officials page: searchable list with bounce rates, opt-out toggle (ADMN-06)
6. Jobs page: link to Bull Board at /api/admin/queues (ADMN-05)
All protected by Clerk admin role.
  </action>
</task>

<task type="auto">
  <name>Task 4: Add auto-escalation check</name>
  <files>apps/worker/src/agents/reconciliation.ts</files>
  <action>
Add auto-escalation to the daily reconciliation job (ADMN-08):
- Check flagged queue depth — if >10 items, send operator alert
- Check oldest flagged item — if >24 hours, send operator alert
- Send via Postmark or console log
  </action>
</task>

</tasks>

<success_criteria>
- Admin dashboard per ADMN-01
- Flagged queue per ADMN-02, ADMN-03
- Treasury dashboard per ADMN-04
- Bull Board per ADMN-05
- Officials maintenance per ADMN-06
- Admin audit logging per ADMN-07
- Auto-escalation per ADMN-08
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-compliance/04-02-SUMMARY.md`
</output>
