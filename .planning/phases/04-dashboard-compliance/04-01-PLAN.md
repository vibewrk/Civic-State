---
phase: 04-dashboard-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/dashboard/page.tsx
  - apps/web/app/dashboard/layout.tsx
  - apps/web/app/dashboard/campaigns/[id]/page.tsx
  - apps/web/components/dashboard/campaign-list.tsx
  - apps/web/components/dashboard/campaign-detail.tsx
  - apps/web/components/dashboard/delivery-status.tsx
  - apps/api/src/routes/campaigns.ts
  - apps/api/src/routes/webhooks.ts
  - apps/api/src/index.ts
  - apps/web/lib/api.ts
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
---

<objective>
Build the user dashboard: campaign list with delivery status, per-letter tracking, reply display, email notifications, and anonymity settings.

Purpose: The dashboard is where users track their investment — seeing delivery confirmations builds trust and drives repeat usage.
Output: Working dashboard with campaign tracking, reply display, and notification preferences.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create campaigns API endpoints</name>
  <files>apps/api/src/routes/campaigns.ts, apps/api/src/index.ts</files>
  <action>
Create apps/api/src/routes/campaigns.ts:
- GET /api/campaigns — returns user's campaigns with delivery status (auth required)
- GET /api/campaigns/:id — returns single campaign with letters, officials, deliveries
- PATCH /api/campaigns/:id/anonymity — toggle anonymity preference (DASH-05)
Register in index.ts.
  </action>
</task>

<task type="auto">
  <name>Task 2: Add Postmark inbound webhook for replies</name>
  <files>apps/api/src/routes/webhooks.ts</files>
  <action>
Add POST /api/webhooks/postmark/inbound handler:
- Parses reply+{campaignId}@civicstate.com from To address (DASH-03)
- Stores reply text in a new reply record or in campaign metadata
- Sends email notification to user about the reply (DASH-04)
  </action>
</task>

<task type="auto">
  <name>Task 3: Build dashboard frontend</name>
  <files>apps/web/app/dashboard/layout.tsx, apps/web/app/dashboard/page.tsx, apps/web/app/dashboard/campaigns/[id]/page.tsx, apps/web/components/dashboard/campaign-list.tsx, apps/web/components/dashboard/campaign-detail.tsx, apps/web/components/dashboard/delivery-status.tsx, apps/web/lib/api.ts</files>
  <action>
1. Dashboard layout with nav header (logo, dashboard link, submit new)
2. Campaign list page showing all campaigns with status badges
3. Campaign detail page with:
   - Per-letter delivery status: queued/sent/delivered/bounced/failed (DASH-02)
   - Official info per letter
   - Reply display as raw text (DASH-03)
   - Anonymity toggle (DASH-05)
4. Delivery status component with color-coded badges
5. Add API functions to web/lib/api.ts
  </action>
</task>

</tasks>

<success_criteria>
- Campaign list per DASH-01
- Per-letter delivery status per DASH-02
- Reply display per DASH-03
- Email notification per DASH-04
- Anonymity toggle per DASH-05
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-compliance/04-01-SUMMARY.md`
</output>
