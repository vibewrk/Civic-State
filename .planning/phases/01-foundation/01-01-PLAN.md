---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - tsconfig.base.json
  - .env.example
  - .npmrc
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/index.ts
  - apps/worker/package.json
  - apps/worker/tsconfig.json
  - apps/worker/src/index.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
# RISK ACKNOWLEDGMENT: 15 files_modified is at blocker threshold but acceptable here.
# Task 2 creates 11 small config stub files (package.json + tsconfig.json per workspace,
# plus 3 one-line index.ts files). Each file is <30 lines of boilerplate. Total context
# cost ~15-20%, well within budget. Splitting would create artificial separation of a
# single coherent operation (workspace scaffolding).
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "pnpm install succeeds from root with all four workspaces resolved"
    - "TypeScript strict mode is enabled across all packages"
    - "packages/shared is importable by apps/api and apps/worker"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definition"
      contains: "apps/*"
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript configuration"
      contains: "strict"
    - path: "packages/shared/src/index.ts"
      provides: "Shared package entry point"
    - path: "apps/api/src/index.ts"
      provides: "Express API entry point"
    - path: "apps/worker/src/index.ts"
      provides: "Worker entry point"
  key_links:
    - from: "apps/api/package.json"
      to: "packages/shared"
      via: "workspace:* dependency"
      pattern: '"shared": "workspace:\\*"'
    - from: "apps/worker/package.json"
      to: "packages/shared"
      via: "workspace:* dependency"
      pattern: '"shared": "workspace:\\*"'
---

<objective>
Scaffold the pnpm monorepo with four workspaces (apps/web, apps/api, apps/worker, packages/shared), shared TypeScript configuration with strict mode, and install all core dependencies per the locked stack.

Purpose: Establishes the foundation that every subsequent plan builds on. No code can be written without the monorepo structure.
Output: Working monorepo where `pnpm install` resolves all workspaces and TypeScript compiles across all packages.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section, Pattern 1: Prisma in pnpm monorepo, pnpm-workspace.yaml example, tsconfig.base.json example, project structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root configuration</name>
  <files>pnpm-workspace.yaml, package.json, tsconfig.base.json, .env.example, .npmrc</files>
  <read_first>pnpm-workspace.yaml (if exists), package.json (root, exists already), .planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
1. Update `pnpm-workspace.yaml` to define workspaces (per D-01):
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

2. Update root `package.json` — preserve existing fields, add:
   - `"private": true`
   - `"name": "civicstate"`
   - `"scripts"`: `"dev": "pnpm -r --parallel run dev"`, `"build": "pnpm -r run build"`, `"lint": "pnpm -r run lint"`, `"typecheck": "pnpm -r run typecheck"`, `"test": "pnpm -r run test"`, `"clean": "pnpm -r run clean"`
   - `"engines": { "node": ">=22.0.0" }`
   - devDependencies: `"typescript": "^6.0.0"`, `"@types/node": "^22.0.0"`

3. Create `tsconfig.base.json` (per D-02, strict mode):
```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "baseUrl": "."
  },
  "exclude": ["node_modules", "dist"]
}
```

4. Create `.env.example` with all environment variables needed for Phase 1:
```
# Database
DATABASE_URL=postgresql://civicstate:password@localhost:5432/civicstate

# Redis
REDIS_URL=redis://localhost:6379

# Clerk
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# Encryption
ENCRYPTION_KEY=hex-encoded-32-byte-key
ENCRYPTION_KEY_VERSION=1
HMAC_SECRET_KEY=

# Anthropic
ANTHROPIC_API_KEY=

# Postmark
POSTMARK_SERVER_TOKEN=
POSTMARK_ACCOUNT_TOKEN=

# Sentry
SENTRY_DSN=https://xxx@sentry.io/xxx
NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx

# Vercel
NEXT_PUBLIC_API_URL=http://localhost:3001

# Agent Config
AGENT_MODEL_OVERRIDE=

# Node
NODE_ENV=development
```

5. Create `.npmrc`:
```
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
```
  </action>
  <verify>
    <automated>cd "/Volumes/Big & Fast/Coding Projects/CivicState" && cat pnpm-workspace.yaml | grep -q "apps/\*" && cat tsconfig.base.json | grep -q '"strict": true' && cat .env.example | grep -q "DATABASE_URL" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm-workspace.yaml contains `apps/*` and `packages/*`
    - tsconfig.base.json has `"strict": true`
    - .env.example lists DATABASE_URL, REDIS_URL, CLERK_SECRET_KEY, ENCRYPTION_KEY, ANTHROPIC_API_KEY, POSTMARK_SERVER_TOKEN, SENTRY_DSN
    - .npmrc exists with shamefully-hoist=false
  </acceptance_criteria>
  <done>Root monorepo configuration files exist with correct workspace and TypeScript settings</done>
</task>

<task type="auto">
  <name>Task 2: Create all four workspace packages with dependencies</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/api/package.json, apps/api/tsconfig.json, apps/api/src/index.ts, apps/worker/package.json, apps/worker/tsconfig.json, apps/worker/src/index.ts, packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts</files>
  <read_first>package.json (root), pnpm-workspace.yaml, .planning/phases/01-foundation/01-RESEARCH.md</read_first>
  <action>
1. Create `packages/shared/package.json`:
```json
{
  "name": "shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./db": "./src/db.ts",
    "./crypto": "./src/crypto.ts",
    "./hmac": "./src/hmac.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "clean": "rm -rf dist generated"
  },
  "dependencies": {
    "prisma": "^7.8.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^22.0.0"
  }
}
```

2. Create `packages/shared/tsconfig.json` extending root:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

3. Create `packages/shared/src/index.ts`:
```typescript
// Re-exports for packages/shared
// Prisma client will be added after schema creation (Plan 02)
export {};
```

4. Create `apps/api/package.json`:
```json
{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec 'tsx src/index.ts'",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "shared": "workspace:*",
    "express": "^5.2.1",
    "@clerk/express": "^2.1.9",
    "cors": "^2.8.6",
    "helmet": "^8.1.0",
    "zod": "^4.3.6",
    "@sentry/node": "^10.50.0",
    "@bull-board/express": "^7.0.0",
    "@bull-board/api": "^7.0.0",
    "bullmq": "^5.76.2",
    "ioredis": "^5.10.1",
    "postmark": "^4.0.7"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^22.0.0",
    "@types/express": "^5.0.0",
    "@types/cors": "^2.8.0",
    "nodemon": "^3.0.0",
    "tsx": "^4.0.0",
    "vitest": "^4.1.5"
  }
}
```

5. Create `apps/api/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

6. Create `apps/api/src/index.ts` — minimal Express server (per D-04, D-17, D-27):
```typescript
import express from 'express';
import cors from 'cors';
import helmet from 'helmet';

const app = express();
const PORT = process.env.PORT || 3001;

app.use(helmet());
app.use(cors({ origin: process.env.CORS_ORIGIN || 'http://localhost:3000' }));
app.use(express.json());

app.get('/api/health', (_req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

app.listen(PORT, () => {
  console.log(`API server running on port ${PORT}`);
});

export default app;
```

7. Create `apps/worker/package.json`:
```json
{
  "name": "worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec 'tsx src/index.ts'",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "shared": "workspace:*",
    "bullmq": "^5.76.2",
    "ioredis": "^5.10.1",
    "@anthropic-ai/sdk": "^0.91.1"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^22.0.0",
    "nodemon": "^3.0.0",
    "tsx": "^4.0.0",
    "vitest": "^4.1.5"
  }
}
```

8. Create `apps/worker/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

9. Create `apps/worker/src/index.ts` — minimal worker bootstrap:
```typescript
console.log('CivicState worker starting...');
// BullMQ workers will be registered in Plan 05 (Agent Engine)
```

10. Create `apps/web/package.json` — minimal, Next.js setup happens in Plan 06:
```json
{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "next lint",
    "clean": "rm -rf .next"
  },
  "dependencies": {
    "next": "^15.5.15",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@clerk/nextjs": "^7.2.7",
    "@sentry/nextjs": "^10.50.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tailwindcss": "^4.2.4"
  }
}
```

11. Create `apps/web/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "noEmit": true,
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

12. Run `pnpm install` from the project root to resolve all workspaces and install dependencies.
  </action>
  <verify>
    <automated>cd "/Volumes/Big & Fast/Coding Projects/CivicState" && pnpm install && pnpm -r exec -- echo "workspace: \$PNPM_PACKAGE_NAME" 2>/dev/null | grep -c "workspace:" | xargs -I{} test {} -ge 4 && echo "PASS: 4+ workspaces found"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` completes without errors
    - `pnpm -r list` shows web, api, worker, shared workspaces
    - apps/api/package.json has `"shared": "workspace:*"` in dependencies
    - apps/worker/package.json has `"shared": "workspace:*"` in dependencies
    - apps/api/src/index.ts exports an Express app listening on port 3001
    - apps/worker/src/index.ts exists with bootstrap log
    - packages/shared/src/index.ts exists as entry point
  </acceptance_criteria>
  <done>All four workspaces created, pnpm install succeeds, cross-workspace dependency from apps/api and apps/worker to packages/shared resolves correctly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Scaffold only, no runtime trust boundaries yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.example | accept | .env.example contains placeholder values only; .gitignore must exclude .env (verified in Plan 03) |
</threat_model>

<verification>
- `pnpm install` succeeds from root
- `pnpm -r run typecheck` succeeds (or no errors on empty packages)
- Four workspaces listed: web, api, worker, shared
</verification>

<success_criteria>
- Monorepo structure matches D-01: apps/web, apps/api, apps/worker, packages/shared
- TypeScript strict mode per D-02
- pnpm workspaces resolve correctly
- Cross-workspace dependency (shared -> api, shared -> worker) works
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
